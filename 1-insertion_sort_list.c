#include "sort.h"

/**
 * insertion_sort_list - function that sorts a doubly linked list of int in ascending order
 * @list: print the list after each time you swap 2 elements
 * Return: void (nothing)
*/

void insertion_sort_list(listint_t **list)
{
	int i;
}
